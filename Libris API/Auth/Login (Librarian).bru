meta {
  name: Login (Librarian)
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "sarah.connor",
    "password": "password"
  }
}

settings {
  encodeUrl: true
}
