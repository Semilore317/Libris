meta {
  name: Login (Member)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "frodo.baggins",
    "password": "password"
  }
}

settings {
  encodeUrl: true
}
